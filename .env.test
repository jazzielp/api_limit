# Safe test environment values. Unit tests mock all external dependencies,
# so this file intentionally uses a placeholder DATABASE_URL.
NODE_ENV=test
PORT=3001
DATABASE_URL=postgresql://postgres:Toluca.01@localhost:5432/api_limit
JWT_SECRET=test-secret-must-be-at-least-32-characters-long
JWT_ISSUER=api_limit_test
JWT_AUDIENCE=api_limit_test_users
MAIL_DRIVER=console
MAIL_FROM=test@example.com
